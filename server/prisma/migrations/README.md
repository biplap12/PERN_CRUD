Run `npx prisma migrate dev --name init` after configuring DATABASE_URL to create the database migration for your local PostgreSQL instance. Prisma migration SQL is environment-specific and is intentionally generated by Prisma rather than hard-coded here.
